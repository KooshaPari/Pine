[package]
name = "pine-compat"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Compatibility layer facade for the Pine runtime"
repository = "https://github.com/KooshaPari/Pine"
readme = "../../README.md"

[dependencies]
pine-core = { path = "../pine-core", version = "0.1.0" }
