[package]
name = "pine-loader"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2021"

[dependencies]
pine-core = { path = "../pine-core" }
goblin = "0.10.7"

[dev-dependencies]
proptest = "1"
